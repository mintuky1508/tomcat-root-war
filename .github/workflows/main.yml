name: Deploy to Tomcat

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Java
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Build Project
      run: |
        ./gradlew build # Ensure gradlew is present in the repository

    - name: Deploy to Tomcat
      env:
        TOMCAT_URL: http://${{ secrets.EC2_PUBLIC_IP }}:8080/manager/text
        TOMCAT_USERNAME: ${{ secrets.TOMCAT_USERNA
